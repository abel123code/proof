export function renderConfirmationDescription(cost: number): string {
  return `Your footage will be sent to the editor for ${cost} credits. Rendering can take a few minutes, and the credits are charged when the job starts.`;
}
